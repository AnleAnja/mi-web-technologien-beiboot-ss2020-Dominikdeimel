version: '3.4'
services:
  frontend:
    build: ../Frontend/imagetool
    ports:
      - "8080:8080"

  backend:
    build: ../Backend
    ports:
      - "3000:3000"
    volumes:
      - type: volume
        source: backendData
        target: /app/userData

volumes:
  backendData:

